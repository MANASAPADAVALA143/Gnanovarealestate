# ============================================
# GNANOVA REAL ESTATE AI ASSISTANT
# Environment Variables Template
# ============================================
# 
# INSTRUCTIONS:
# 1. Copy this content to your .env or .env.local file
# 2. Replace all REPLACE_ME values with your actual credentials
# 3. Get credentials from the sources listed below
# 4. Save and restart terminal/IDE
#
# ============================================

# Step 1 — Use real Supabase data in the dashboard (not preview user / empty lists):
# Set false, then restart `npm run dev`. You will need to sign in.
VITE_DASHBOARD_PREVIEW=false

# Webhook server (node webhook-server.js): assign inbound VAPI calls to your broker when VAPI does not send metadata.agentId.
# Must equal public.agents.id (usually the same UUID as your Supabase auth user id for a solo agent).
# Also used as fallback for outbound call.ended webhooks missing metadata.
# DEFAULT_AGENT_ID=
# VITE_DEFAULT_AGENT_ID=

# ============================================
# REQUIRED: Supabase Database
# ============================================
# Get from: https://supabase.com/dashboard → Your Project → Settings → API
# Project URL: Copy "Project URL" field
# Service Role Key: Copy "service_role" key (NOT anon key!)

SUPABASE_URL=https://REPLACE_ME.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.REPLACE_ME

# REQUIRED for Sign in / Sign up in the web app (browser). Same project as above — from Supabase → Settings → API copy "anon" "public" key (NOT service_role).
VITE_SUPABASE_URL=https://REPLACE_ME.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.REPLACE_ME_ANON

# ============================================
# REQUIRED: OpenAI API (for embeddings & AI)
# ============================================
# Get from: https://platform.openai.com/api-keys
# Create new API key if needed
# Cost: ~$0.0001 per 1K tokens

OPENAI_API_KEY=sk-proj-REPLACE_ME

# ============================================
# REQUIRED: Anthropic API (for Claude AI)
# ============================================
# Get from: https://console.anthropic.com/settings/keys
# Used for Listing Writer feature
# Cost: ~$0.015 per document parse, ~$0.006 per listing generation

ANTHROPIC_API_KEY=sk-ant-api03-REPLACE_ME
VITE_ANTHROPIC_API_KEY=sk-ant-api03-REPLACE_ME

# ============================================
# OPTIONAL: VAPI Voice AI (for phone calls)
# ============================================
# Get from: https://dashboard.vapi.ai → Settings → API Keys
# Only needed if testing AI voice calls

VAPI_API_KEY=REPLACE_ME
VAPI_PHONE_NUMBER_ID=REPLACE_ME
VAPI_SERVER_SECRET=REPLACE_ME
VAPI_INBOUND_PHONE_NUMBER=+1234567890

# ============================================
# OPTIONAL: Twilio WhatsApp (for messaging)
# ============================================
# WhatsApp — Meta Cloud API (active)
# ============================================
# Get token from: Meta Developer Portal → your app → WhatsApp → API Setup → "Send messages" panel
# Phone Number ID and WABA ID are fixed for this account
WHATSAPP_TOKEN=your_meta_system_user_token_here
WHATSAPP_PHONE_NUMBER_ID=1298685323329504
# WABA_ID=955765654192959  (reference only, not needed in code)
# APP_ID=2235996580596817  (reference only, not needed in code)

# ============================================
# Twilio — Voice/SMS only (WhatsApp migrated to Meta above)
# ============================================
# Get from: https://console.twilio.com → Account Info
TWILIO_ACCOUNT_SID=ACREPLACE_ME
TWILIO_AUTH_TOKEN=REPLACE_ME
# SMS agent alerts (speed + outbound scoring) and 24h nudge cron — E.164
TWILIO_PHONE_NUMBER=+15707084979
# LEGACY — no longer used for WhatsApp outbound (kept for inbound TwiML fallback only)
# TWILIO_WHATSAPP_FROM=whatsapp:+1234567890
# Public ngrok URL + path for Twilio inbound WhatsApp webhook signature validation
TWILIO_WEBHOOK_URL=https://your-ngrok-url/webhook/whatsapp/inbound

# ============================================
# OPTIONAL: Application URLs
# ============================================
# For local development, defaults work fine
# For production, set to your domain

APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# OPTIONAL: n8n Webhook (for automation)
# ============================================
# Only if using n8n for workflows

N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/REPLACE_ME

# ============================================
# Speed-to-Lead: universal portal intake (n8n → Next.js)
# ============================================
# POST /api/leads/portal-intake with header: x-webhook-secret: <same value>
# Use a long random string in production.

WEBHOOK_SECRET=REPLACE_ME

# ============================================
# OPTIONAL: Server Configuration
# ============================================
# Usually don't need to change these

PORT=3001
EXPRESS_BASE_URL=http://localhost:3001
NEXT_API_BASE_URL=http://localhost:3000

# ============================================
# NEW: GoHighLevel CRM Integration
# ============================================
# Get from: https://highlevel.com → Settings → API
# Automatically syncs all leads to your GHL CRM
# Get Location ID from: Settings → Business Profile

GHL_API_KEY=REPLACE_ME
GHL_LOCATION_ID=REPLACE_ME

# ============================================
# NEW: Facebook Lead Ads Integration
# ============================================
# Get from: https://developers.facebook.com → Your App
# Verify Token: Create a random string (e.g., "gnanova_verify_2025")
# App Secret: Found in App Dashboard → Settings → Basic

FACEBOOK_VERIFY_TOKEN=gnanova_verify_token_2025
FACEBOOK_APP_SECRET=REPLACE_ME

# ============================================
# NEW: Integration Settings (Optional)
# ============================================
# Used by the Integrations dashboard page

VITE_GHL_API_KEY=$GHL_API_KEY
VITE_GHL_LOCATION_ID=$GHL_LOCATION_ID
VITE_FACEBOOK_VERIFY_TOKEN=$FACEBOOK_VERIFY_TOKEN
VITE_FACEBOOK_APP_SECRET=$FACEBOOK_APP_SECRET
VITE_VAPI_INBOUND_PHONE_NUMBER=$VAPI_INBOUND_PHONE_NUMBER
